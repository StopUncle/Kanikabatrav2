# IMPORTANT: PayPal Configuration Setup Instructions
# ================================================
# This file contains the required environment variables for the website to function properly.
# Copy this file to .env and fill in the actual values.

# Database (Optional - only if using database features)
DATABASE_URL="postgresql://user:password@localhost:5432/kanikabatra?schema=public"

# JWT Secrets (Required for authentication)
# Generate secure random strings for production using: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# PayPal Configuration (REQUIRED for payment processing)
# ======================================================
# To get these credentials:
# 1. Go to https://developer.paypal.com/dashboard/
# 2. Create an account if you don't have one
# 3. Click on "Apps & Credentials"
# 4. For testing: Use "Sandbox" tab
# 5. For production: Use "Live" tab
# 6. Click "Create App" and give it a name (e.g., "Kanika Batra Website")
# 7. Copy the Client ID and Secret from the app details

# PayPal Client ID (visible in browser - public)
NEXT_PUBLIC_PAYPAL_CLIENT_ID="YOUR_PAYPAL_CLIENT_ID_HERE"
PAYPAL_CLIENT_ID="YOUR_PAYPAL_CLIENT_ID_HERE"

# PayPal Client Secret (keep secret - server only)
PAYPAL_CLIENT_SECRET="YOUR_PAYPAL_CLIENT_SECRET_HERE"

# PayPal Webhook ID (optional - for payment notifications)
PAYPAL_WEBHOOK_ID="YOUR_WEBHOOK_ID_HERE"

# Email Configuration (Optional - for sending contact form emails)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_SECURE="false"
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="your-app-specific-password"
EMAIL_FROM="\"Kanika Batra\" <your-email@gmail.com>"
ADMIN_EMAIL="kanika@kanikarose.com"

# Application URLs
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Environment
NODE_ENV=development

# INSTRUCTIONS:
# 1. Copy this file and rename it to .env
# 2. Replace all placeholder values with actual credentials
# 3. For PayPal testing, use Sandbox credentials
# 4. For production deployment, use Live PayPal credentials
# 5. Never commit the .env file to version control