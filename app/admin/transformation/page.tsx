import TransformationClient from "./TransformationClient";

export const metadata = {
  title: "The 12 Weeks | Admin",
};

export default function AdminTransformationPage() {
  return <TransformationClient />;
}
