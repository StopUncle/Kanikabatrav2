import DonateClient from "./DonateClient";

export const metadata = {
  title: "Support Kanika — Donate",
  description:
    "Support Kanika's work — the books, the simulator, the community, the writing. Pay what you want.",
};

export default function DonatePage() {
  return <DonateClient />;
}
