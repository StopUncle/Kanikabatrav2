/**
 * Climax - Social Proof
 * Mission 5: The big event where all paths converge + Maris cameo
 */
import { eventScenes } from './the-event';
import { marisSScenes } from './maris-appears';
import { bridgeScenes } from './the-bridge';

export const climaxScenes = [...eventScenes, ...marisSScenes, ...bridgeScenes];
