/**
 * Ex Path - The Ex Returns
 * Mission 4: Navigate Marcus (Dismissive Avoidant) and triangulation
 */
import { appearanceScenes } from './appearance';
import { historyScenes } from './history';
import { resolutionScenes } from './resolution';

export const exScenes = [
  ...appearanceScenes,
  ...historyScenes,
  ...resolutionScenes,
];
