/**
 * Mission 15: The Final Choice
 * Convergence. Every relationship tested.
 */
import { convergenceScenes } from './the-convergence';
import { revelationScenes } from './the-revelation';
import { decisionScenes } from './the-decision';
import { bridgeScenes } from './the-bridge';

export const climaxScenes = [
  ...convergenceScenes,
  ...revelationScenes,
  ...decisionScenes,
  ...bridgeScenes,
];
