/**
 * Ghost Path - Past relationships return
 * Kai and Casey are fixed; Marcus and Dana are dynamic (based on L3)
 */
import { kaiScenes } from './ghost-kai';
import { caseyScenes } from './ghost-casey';

export const ghostScenes = [...kaiScenes, ...caseyScenes];
